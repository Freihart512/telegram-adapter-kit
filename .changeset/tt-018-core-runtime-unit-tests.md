---
"telegram-adapter-kit": patch
---

Formalize core runtime test coverage for `TT-018` by adding a dedicated `test:coverage` script, wiring Vitest V8 coverage reporting (`text`, `json-summary`, `html`), and documenting how to generate and review coverage reports from `coverage/index.html`.
