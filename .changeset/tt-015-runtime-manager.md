---
"telegram-adapter-kit": patch
---

Expose `RuntimeManager` and `createRuntimeManager` implementing `TelegramRuntimeSdk`, orchestrating `BotRegistry`, `SubscriptionRegistry`, `EventBus`, and `TelegramProviderAdapter` (`TT-015`).
