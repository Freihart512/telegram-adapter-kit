---
"telegram-adapter-kit": patch
---

Add maxDelayMs and jitter to RetryPolicy (TT-048), optional operation backoff overlays, and named presets. Default retry timing unchanged unless overlays are opted in.
