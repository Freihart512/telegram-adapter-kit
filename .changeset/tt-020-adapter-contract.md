---
"telegram-adapter-kit": patch
---

Refine the internal `TelegramProviderAdapter` contract documentation and add dedicated type-level tests for adapter capabilities, lifecycle/send/binding method completeness, and normalized incoming callback signatures (`TT-020`).
