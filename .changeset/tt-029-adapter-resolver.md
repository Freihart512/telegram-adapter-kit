---
"telegram-adapter-kit": minor
---

Add `DefaultTelegramAdapterResolver` and `createDefaultTelegramAdapterResolverForRegistry` to route MTProto vs Bot API by `credentials.kind` / `runtimeKind` (TT-029).
