{
  "name": "telegram-adapter-kit",
  "version": "0.0.0",
  "description": "TypeScript toolkit for Telegram: runtime bot registration, subscriptions, normalized messages, MTProto (GramJS) and Bot API (grammY).",
  "keywords": [
    "telegram",
    "gramjs",
    "grammY",
    "mtproto",
    "bot-api",
    "typescript"
  ],
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "node -e \"console.log('[TT-005] Build will be configured in TT-005.');\"",
    "test": "node -e \"console.log('[TT-004] Tests will be configured in TT-004.');\"",
    "lint": "node -e \"console.log('[TT-003] Lint will be configured in TT-003.');\"",
    "typecheck": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
